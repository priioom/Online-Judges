#includestdio.h
#includemath.h
#includestdlib.h
 
int main()
{
    long long int n,m,a,sum,nai,temp;
    scanf(%I64d %I64d %I64d,&n,&m,&a);
    sum=ceil( (double) na);
    nai=ceil((double)ma);
    printf(%I64dn,sumnai);
    return 0;
}